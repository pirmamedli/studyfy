# Supabase — заполни и переименуй в .env.local (значения из Project Settings → API)
# Без этих переменных приложение работает в local-first режиме (без облака).
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
